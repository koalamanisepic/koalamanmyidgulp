<?php
/**
 * Change this before using the French sentence log. Anyone who knows
 * this passphrase can add entries to the log at learning/french/ —
 * it is a lightweight deterrent against casual/bot spam, not real
 * authentication, so don't reuse a password you use elsewhere.
 */
if (!defined('FRENCH_LOG_PASSPHRASE')) {
    define('FRENCH_LOG_PASSPHRASE', 'change-me');
}
